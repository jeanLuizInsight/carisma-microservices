server:
  port: 8888
spring:
  application:
    name: carisma-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/jeanLuizInsight/carisma-config-server
          #username: username
          #password: password
          default-label: main
          search-paths:
          - 'greeting-service*'